{
  "name": "esbuild-plugin-demo",
  "version": "1.0.0",
  "description": "esbuild plugin demo",
  "author": "",
  "license": "MIT",
  "keywords": [
    "esbuild",
    "plugin"
  ],
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --fix ."
  },
  "dependencies": {
    "esbuild": "^0.24.0"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^3.11.2",
    "@types/node": "^22.10.0",
    "eslint": "^9.16.0"
  }
}
